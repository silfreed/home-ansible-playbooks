@version: 3.38
@include "scl.conf"

source s_network {
    default-network-drivers();
};

destination d_loki {
  syslog("promtail-syslog" transport("tcp") port(1514));
};

log {
  source(s_network);
  destination(d_loki);
};